services:
  report-self:
    build: .
    ports:
      - "8013:8013"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8013/health')"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 5s
